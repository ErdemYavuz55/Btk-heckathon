// Minimal Plotly.js placeholder for local development
window.Plotly = {
  newPlot: function(element, data, layout, config) {
    // Create a simple chart div
    element.innerHTML = `
      <div style="
        width: 100%; 
        height: 400px; 
        border: 2px solid #3b82f6; 
        border-radius: 8px; 
        display: flex; 
        flex-direction: column; 
        align-items: center; 
        justify-content: center; 
        background: linear-gradient(135deg, #f8fafc 0%, #e2e8f0 100%);
        font-family: system-ui, sans-serif;
      ">
        <div style="font-size: 24px; font-weight: bold; color: #1e40af; margin-bottom: 16px;">
          📊 ${layout?.title || 'Interactive Simulation'}
        </div>
        <div style="font-size: 16px; color: #64748b; text-align: center; max-width: 80%;">
          <p><strong>Data Points:</strong> ${data?.[0]?.y?.length || 'N/A'}</p>
          <p><strong>X-Axis:</strong> ${layout?.xaxis?.title || 'Time'}</p>
          <p><strong>Y-Axis:</strong> ${layout?.yaxis?.title || 'Value'}</p>
          ${data?.[0]?.y ? `<p><strong>Range:</strong> ${Math.min(...data[0].y).toFixed(2)} - ${Math.max(...data[0].y).toFixed(2)}</p>` : ''}
        </div>
        <div style="
          margin-top: 20px; 
          padding: 12px 24px; 
          background: #3b82f6; 
          color: white; 
          border-radius: 6px; 
          font-size: 14px;
        ">
          🚀 Simulation Running Successfully!
        </div>
      </div>
    `;
    console.log('Plotly.newPlot called with data:', data, 'layout:', layout);
  },
  
  react: function(element, data, layout, config) {
    return this.newPlot(element, data, layout, config);
  },
  
  redraw: function(element) {
    console.log('Plotly.redraw called');
  }
};

console.log('✅ Local Plotly placeholder loaded successfully!'); 